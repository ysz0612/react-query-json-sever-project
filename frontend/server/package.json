{
  "name": "json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 3001"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}